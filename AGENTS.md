# 项目开发规则

## 交付范围

本项目是独立的本地 Codex 用量与 Skills 证据分析工具。不要复制授权不明确的上游代码。未经明确需求，不新增网页登录、远程管理、模型调用、遥测、广告或自动更新。

## 文档

说明性文档必须中文，命令、代码标识符与必要术语保留原文。规格与任务分别存放在 `docs/specs/`、`docs/tasks/`，文件名包含日期。

## 安全

原始 Codex 数据只读；缓存只写本应用目录。不得读取 auth.json、Cookie 或 Keychain，不将 Prompt、助手正文、工具输出或原始日志片段写入缓存。不要退出或重启 Codex。安装不得关闭 Gatekeeper 或移除 quarantine。

## 数据口径

缓存输入是输入的子集，推理输出是输出的子集。累计计数不可直接逐行相加。对话 Fork 与子代理委派是两种关系。父、子和总计必须能对账，丢失数据必须标注。Skill 被提及、被读取、真正有效是不同事实。不能把关联会话 Token 称为 Skill Token。

## 实施与验证

每个任务只有一个主要交付目标。解析、聚合、界面、安装和真实 Mac 验收分开记录；没有在 Mac 编译就不能把语法解析说成桌面验证通过。

改动前读取对应实现与测试；改动后执行 `./scripts/test.sh`。涉及 SwiftUI / AppKit 时还必须在 Mac 执行 `./scripts/build.sh` 并人工验收。只输出必要日志，不向模型上传真实账号文件或完整对话。
