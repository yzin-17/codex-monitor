# 安全边界

日期：2026-09-10

## 默认读取

只在选定 Codex 数据目录中读取：

- `sessions/**/rollout-*.jsonl`、`archived_sessions/**/rollout-*.jsonl`。
- 最高版本 `state_*.sqlite` 的标题、路径、父子关系，以及 `session_index.jsonl`。
- 用户和选定项目下的 `SKILL.md`，或用户明确添加的 Skills 目录。

JSONL 根目录与文件不能通过符号链接越出选定 Codex 根目录。Skill 目录允许用户自己的符号链接，因为这属于常见组织方式；遍历有循环检测、深度与条目上限。请不要添加不可信的目录。

不读取 `auth.json`、密码、Token、浏览器 Cookie、Keychain；没有 HTTP 客户端、CDP、WebView 或运行时 Shell/Process 调用。工具名称只是离线分析日志中的字符串，绝不会执行日志里的命令。

## 写入

只写入本应用目录：设置、用户导入价格、扫描检查点、聚合计数和证据元数据。不得把缓存放到 Codex 数据目录之内。目录为 0700，文件为 0600，使用受限临时文件和原子 rename。

缓存保存会话 ID、Skill 路径、当前模型、累积计数、时间、偏移和文件变化指纹。文件变化指纹不是匿名化手段。不保存原始 Prompt、回答、工具参数文本、工具输出或日志尾部字节。内存解析时会接触日志正文来识别 Skill 证据，随后丢弃。

即使没有密码，缓存仍包含私人开发活动，不能当作公开文件分享。文件权限不是加密，也不能阻止同一用户权限下的恶意程序或管理员访问。

## 用户触发的系统行为

只有点击“打开对话”才调用 `NSWorkspace` 打开经过 UUID 校验的 `codex://threads/<id>`；不附加 Prompt、不发送消息。源日志按钮只打开 Finder 定位，不执行日志。文件选择、报告导出由用户确认。

Codex 或其他系统应用在被用户打开后可能自行联网。这不等于本程序包含上传接口。

## 安装与更新

构建只有本地 Swift 编译与系统 SQLite，没有第三方 Swift 包、远程下载脚本或自动更新。安装不重启 Codex、不删除其文件、不调整登录配置、不清除 quarantine。替换旧 Codex Monitor 需要明确 `--replace`，旧应用会备份。

应用默认 ad-hoc 签名，未做 Apple 公证、未启用 App Sandbox。静态 API 检查不能证明恶意修改绝无其他外传路径。应审查你实际构建的提交，而不是永久信任项目名或这份说明。

## 已知限制

此交付没有进行 macOS 抓包、二进制反编译或真实账号渗透测试。没有提供“绝对安全”保证。SQLite 只读事务不表示能控制系统 SQLite 内部所有 WAL/SHM 操作。

导出隐藏对话 ID、标题和路径，但仍保留 Skill 名称、模型名和用量。发送给第三方之前应自行核对。
