# 展开面板与费用明细任务

日期：2026-09-10。对应规格：`../specs/2026-09-10-expanded-panel-cost-details.md`。状态：T09、T10、T11、T12、T13 本地实现完成，T08 真实运行态复验待执行。

- [x] T01：共用展开几何，目标 680 × 720、内容缩放 1.4，限制屏幕和 Dock 边界；紧凑刘海不变。
- [x] T02：代理各自用量、递归关系、Fork/继承和归档去重；缺失部分不猜价。
- [x] T03：完成首版只读分片加载、32 MiB/4 秒单轮预算、继续扫描、取消及展开区内存游标；该历史实现由 T09 收敛为自动持续扫描。
- [x] T04：成功读取证据与回合费用关联；同回合多个 Skill 不重复加入任务合计。
- [x] T05：行内展开、独立 Token 弹窗、切页或收起取消扫描，保留全部原版标签。
- [x] T06：27 项本地隔离回归测试、Swift 语法及来源门禁通过。
- [x] T07：macOS 完整编译、123 项测试、原版独立回归、五张原生截图与双架构打包通过；证据见 `../VALIDATION-panel-costs.md`。
- [ ] T08：真实 Mac 交互和日志对账；不把 CI 或合成截图当实机验收。
  - 覆盖验收标准：AC1、AC3、AC4、AC5、AC6 的真实日志与原生界面断言。
  - 依赖：T09。
  - 完成条件：真实父子代理日志跨压缩合计正确，长日志自动追平且界面持续可交互，收起再展开能够续读。
  - 验证方式：安装当前工作区构建，在真实日志上记录压缩前后 Token 对账、扫描进度、取消/续读和原生界面证据。
  - 2026-09-13 首轮真实证据：安装 App 的可执行文件与当前 `dist/CodexMonitor.app` SHA-256 一致；真实展开截图中六行代理 Token 与六份日志累计值逐项对齐，合计一致，且无可归属数据时 Skill 费用区域已隐藏。门禁未通过：三份含 1.28–2.07 MiB `compacted` 行的日志被误报为缺口，通用诊断仍残留 Skill 归属说明；收起续读仍待修复后复验。
- [x] T09：修正子代理首次归属与跨压缩累计，实施无累计总量上限的后台轮转扫描、可续读内存缓存和明确进度状态。
  - 覆盖验收标准：AC1、AC2、AC3、AC4、AC5；AC6 的受影响回归。
  - 依赖：无。
  - 涉及范围：`ConversationCostLoader`、费用明细状态与 SwiftUI 展示、`UsageViewModel` 的内存缓存以及对应测试；不改变列表快照来源、价格表、远程能力或日志只读边界。
  - 完成条件：同一子代理跨压缩费用不丢失；扫描在后台自动追平并持续发布进度；收起后游标可恢复；无用量与扫描中状态可区分。
  - 验证方式：定向费用测试覆盖继承前缀、多次压缩、计数重复/回退、大文件轮转、进度、取消、收起续读和空日志；随后执行项目测试、构建及受影响原生截图测试。
  - 验证证据：通过；`rtk swift test --filter ConversationCostsTests`（35 项）、`rtk swift test`（258 项）、`rtk swift test --filter referenceNativeTabsRenderFromActualUpstreamViews`（1 项）、`rtk ./scripts/test.sh`、`rtk ./scripts/build.sh` 和 `git diff --check` 均退出码 0；双架构 App/DMG 构建成功。证据基于当前未提交工作树，证明本地实现、合成日志语义、原生截图入口和构建，不代替 T08 的真实日志与安装后交互验收。
- [x] T10：没有可归属 Skill 费用时隐藏整个 Skill 费用区域，仅在存在可归属行时显示局部证据与说明。
  - 覆盖验收标准：AC7；AC6 的受影响原生布局断言。
  - 依赖：T09。
  - 涉及范围：费用明细 SwiftUI 条件渲染、Skill 专属诊断及对应测试；不删除底层 Skill 证据提取能力，不改变代理费用或任务合计。
  - 完成条件：无 Skill 行、Skills 关闭和全部 Skill 证据不可归属时均不显示该区域；存在行时显示已有数据，且局部缺口不污染代理费用诊断。
  - 验证方式：定向状态/渲染测试、完整 Swift 测试、受影响原生截图测试与构建。
  - 验证证据：通过；新增状态测试覆盖“无行隐藏、有行显示、局部缺口仅附着于有数据区域”。`rtk swift test --filter ConversationCostsTests`（36 项）、`rtk swift test --filter referenceNativeTabsRenderFromActualUpstreamViews`（1 项）、`rtk swift test`（259 项）、`rtk ./scripts/test.sh`、`rtk ./scripts/build.sh` 和 `git diff --check` 均退出码 0；arm64、amd64 DMG 与通用 App 构建成功。证据基于当前未提交工作树，不代替 T08 的安装后真实界面验收。
- [x] T11：修复真实日志中的超大 `compacted` 行缺口误报，并移除无 Skill 行时通用诊断里的 Skill 归属残留。
  - 覆盖验收标准：AC4、AC7。
  - 依赖：T09、T10；由 T08 首轮真实日志验收触发。
  - 涉及范围：费用明细 JSONL 行分类、缺口诊断文案和对应合成回归；不放宽未知超大行、费用相关超大行或损坏记录的 fail-closed 行为。
  - 完成条件：可从有限前缀确认的超大 `compacted` 行按无费用证据行跳过且不产生代理/Skill 缺口；未知超大行仍报告缺口；无 Skill 行的通用诊断不出现 Skill 文案。
  - 验证方式：定向费用测试覆盖单工作片与跨工作片超大压缩行、未知超大行和诊断文案；随后执行完整测试、原生截图测试与构建门禁。
  - 验证证据：通过；`rtk swift test --filter ConversationCostsTests`（38 项）、原生截图测试（1 项）、`rtk ./scripts/test.sh` 与 `rtk ./scripts/build.sh` 均退出码 0，两个仓库门禁各自完成 261 项全量回归；arm64、amd64 DMG 与通用 App 构建成功，`git diff --check` 通过。单独并行执行 `rtk swift test` 时曾出现无关原生替身进程超时，失败项逐项复跑均通过，且随后两个完整仓库门禁未复现。真实超大行前缀核对确认外层 `compacted` 类型位于 `payload` 之前，覆盖当前运行日志形状；安装后显示仍由 T08 复验。

- [x] T12：列表费用明细有界后台预取，并让顶部任务卡与展开区共享已完成的代理合计。
  - 覆盖验收标准：AC8；AC3、AC4、AC5、AC6 的受影响范围。
  - 依赖：T09、T10、T11。
  - 涉及范围：`UsageViewModel` 的预取队列与共享状态、任务卡和展开明细的数据传递、费用状态判断及定向测试；不改变 Token 价格表、日志解析去重规则、Skill 非独占口径、远程能力或只读边界。
  - 完成条件：列表刷新后最多选择 3 个运行中/最近任务并串行后台预取；完整、无缺口、可定价的明细在未展开前更新顶部 Token 与金额；扫描中、有缺口或未定价时保留快速快照；展开不启动同一任务的重复并行扫描且能复用已有结果；切页或收起面板会取消未完成预取并保留已完成缓存；缓存、预取和取消不阻塞主线程。
  - 验证方式：定向费用与共享状态测试覆盖候选上限与优先级、已完成/未完成覆盖条件、预取发布和顶部复用；实现路径覆盖单 worker 串行、切页/收起取消及重新进入恢复。随后执行完整 Swift 测试、`scripts/test.sh`、`scripts/build.sh`、受影响原生截图测试及 `git diff --check`。
  - 验证证据：通过；`rtk swift test --filter ConversationCostsTests`（41 项）、`rtk ./scripts/test.sh`（266 项、3 个测试套件，含 `referenceNativeTabsRenderFromActualUpstreamViews` 原生截图测试，独立 regression tests 通过）、`CODEX_NOTCH_SKIP_TESTS=1 rtk ./scripts/build.sh`（arm64/amd64 DMG 与通用 App 构建并签名校验通过）和 `rtk git diff --check` 均退出码 0。证据基于当前未提交工作树，证明本地实现、合成日志语义、原生截图入口和构建，不代替 T08 的真实日志、安装后交互与长期性能验收。

- [x] T13：统一顶部与明细的已观察费用口径，并显式呈现不完整结果的下限语义。
  - 覆盖验收标准：AC9；AC4、AC8 的受影响范围。
  - 依赖：T12。
  - 涉及范围：费用明细终态判断、顶部共享展示模型、Token/金额格式和差额说明及对应测试；不改变日志归属解析、价格表、预取上限、远程能力或只读边界。
  - 完成条件：完整明细继续以完整估算替换快速快照；已追平且归属可确认的缺口明细以可确认下限替换快速快照，顶部与展开区 Token、金额一致并显示 `≥`；扫描中、不可读和归属未确认状态仍保持快速快照。
  - 验证方式：定向测试覆盖完整、缺口、扫描中、不可读和归属未确认五类状态及 `≥` 格式；随后执行费用测试、完整项目门禁、构建和 `git diff --check`。真实会话 `01a0a5ae…` 的安装后显示由 T08 复验。
  - 验证证据：通过；费用定向测试 43 项通过，其中新增回归证明缺口明细追平后由 `UsageViewModel` 作为共享 `lowerBound` 提供给顶部与展开区，扫描中和不可用状态不替换快速快照，`≥` 金额格式通过。`rtk ./scripts/test.sh` 通过 268 项测试（3 个套件，含原生截图入口和独立 regression tests）；`CODEX_NOTCH_SKIP_TESTS=1 rtk ./scripts/build.sh` 完成 arm64/amd64 DMG 与通用 App；`codesign --verify --deep --strict dist/CodexMonitor.app` 和 `rtk git diff --check` 通过。证据基于当前未提交工作树，未覆盖安装中 App 的真实界面。

## 验收映射

| AC / 断言 | 实现责任任务 | 验证责任任务或门禁 |
| --- | --- | --- |
| AC1 / 跨压缩合并且不重复计费 | T09 | T09；真实日志由 T08 对账 |
| AC2 / 代理身份和继承隔离 | T09 | T09 |
| AC3 / 无累计上限、后台轮转和取消 | T09 | T09；真实交互由 T08 验证 |
| AC4 / 进度、终态与缺口分类 | T09、T11 | T09、T11；原生界面由 T08 验证 |
| AC5 / 收起续读与只读边界 | T09 | T09；真实交互由 T08 验证 |
| AC6 / 全面布局和历史回归 | T09（受影响范围） | T07 历史证据；T08 当前真实门禁 |
| AC7 / 无数据隐藏、有数据保留 | T10、T11 | T10、T11；真实界面由 T08 验证 |
| AC8 / 列表预取与顶部/展开共享完整费用 | T12 | T12；真实交互与长期性能由 T08 验证 |
| AC9 / 已追平明细统一口径与下限语义 | T13 | T13；真实日志与安装后界面由 T08 验证 |

## 最终一致性 Review

- [ ] Spec 的全部验收断言均有明确实现与适当验证
- [x] 所有已勾选任务满足自身完成条件且证据仍有效
- [ ] 必要集成、真实运行态与条件性门禁均已通过或有合法不适用依据
- [x] 启动依赖、验收依赖与契约就绪证据正确且无循环
- [x] 跨任务接口、类型、状态、时间、错误与副作用语义一致（如适用）
- [x] 不存在未解决的 Blocking 问题、占位要求或未定义契约
- [x] 实现未超出 Spec 范围，未把可选设施变成强制要求
- [x] 证据类型、场景覆盖、代码版本与目标环境支持所声明的验收结果
- [x] 测试、配置、文档、Spec/Task 状态与实际实现一致
- [x] 必要实施 Step 已验证；提交处理符合授权且保留既有用户修改
- [x] 未发现未处理的实现、Spec、任务或验收证据不一致

### Review 结论

- 结论：T09、T10、T11、T12、T13 本地实现范围通过；功能级最终 Review 尚未通过，T08 需在安装当前构建后复验。
- 已确认的问题及责任任务：子代理跨压缩重置、扫描预算闲置、游标随展开区销毁和状态文案混淆已由 T09 修复；Skill 费用区域隐藏已由 T10 修复；超大 `compacted` 行误报和通用诊断 Skill 残留已由 T11 修复；已追平缺口明细仍保留截断快速快照、造成顶部与展开区数值不一致的问题已由 T13 修复，并以 `≥` 保留 fail-closed 语义。
- 尚未通过的必要门禁与阻塞原因：当前运行 App 未因本次工作树构建而自动替换；安装当前构建后，T08 还需复验合法缺口以 `≥` 呈现且顶部/明细一致、收起续读、预取对真实日志的准确性和原生交互。
- 遗留风险或已确认的后续范围：运行中日志持续增长时以“追平观察点”而不是永久 EOF 作为进度终态。
- 验证命令/过程、结果与证据引用：T09、T10、T11、T12 的历史证据仍保留；当前 T13 使用定向费用测试 43 项、完整回归 268 项（含原生截图入口）、独立 regression tests、双架构构建、签名校验和 `git diff --check`，均通过。T08 首轮使用当前安装 App、用户提供的真实展开截图与六份只读日志逐项对账；当前实现仍需安装后确认顶部与展开区统一显示同一个 `≥` 下限，并复验收起续读、预取准确性和原生交互，保持未勾选。
