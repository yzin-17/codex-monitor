# Codex 当前账号本机优先刷新实施任务

对应 Spec：[2026-09-18-codex-local-first-account-refresh.md](../specs/2026-09-18-codex-local-first-account-refresh.md)
> 状态：三模式本地实施完成；真实账户切换与长期运行为独立实机门禁

## 任务

- [x] T1：传播真实额度采集时间
  - 覆盖验收标准：AC1
  - 依赖：无
  - 涉及范围：`UsageSnapshot`、本机额度读取/缓存/稳定化与外部快照输出回归测试。
  - 完成条件：所有复用路径保留原采集时间，旧缓存会过期，外部 JSON 不新增该字段。
  - 验证方式：采集时间定向 Swift 测试与快照输出断言。
  - 验证证据：通过；`RateLimitSourceTests` 与 `stabilizedQuotaKeepsTheOriginalCaptureTimeAndExternalJSONOmitsIt` 覆盖实时、失败缓存、稳定化与外部 JSON 边界。

- [x] T2：实现 Store 本机状态与自动刷新策略
  - 覆盖验收标准：AC2、AC3
  - 依赖：T1 的采集时间契约
  - 涉及范围：`CodexAccountsStore` 的本机快照状态、身份切换、自动判定、即时兜底及现有队列去重；不改变官方协议和持久化。
  - 完成条件：单/多/未绑定账号、缺失/过期、重复不可用、身份切换、手动强制与去重场景全部可独立断言。
  - 验证方式：使用合成本机身份、替身 Vault 与请求计数客户端的定向 Swift 测试。
  - 验证证据：通过；`CodexLocalFallbackTests` 覆盖单账号零请求、多账号、未绑定、即时兜底、重复不可用、身份切换、手动强制与去重。

- [x] T3：控制器接线与统一本机优先展示
  - 覆盖验收标准：AC2、AC4
  - 依赖：T1、T2
  - 涉及范围：控制器订阅首次本机发布状态；Store 统一展示数据；HUD 和 `CodexAccountsPanel` 消费；对话费用 WIP 仅做最小兼容增量。
  - 完成条件：当前匹配账号始终优先显示有效本机额度并标记“本机读取”，远程专属字段并存，本机不可用才使用远程额度。
  - 验证方式：展示策略单元测试和生产 SwiftUI 合成账号截图测试。
  - 验证证据：通过；控制器从 `UsageViewModel.$snapshot.dropFirst()` 接线；原生合成截图显示 HUD“Codex · 本机读取”、本机额度，以及账号卡片中的本机额度、远程 plan、Credits 和远程时间。

- [x] T4：完成本地回归与构建门禁
  - 覆盖验收标准：AC5
  - 依赖：T1、T2、T3
  - 涉及范围：当前未提交工作树；不使用真实凭据或联网账号，不安装或替换运行中的应用。
  - 完成条件：定向 Swift 测试、`scripts/test.sh`、原生截图测试、`scripts/build.sh`、`git diff --check` 均通过并记录证据。
  - 验证方式：执行上述入口；区分合成测试、构建与独立实机门禁。
  - 验证证据：通过；定向策略与采集时间测试通过；`scripts/test.sh` 通过 278 项 Swift 测试及独立回归；原生截图测试通过并人工检查关键截图；`scripts/build.sh` 通过并生成 arm64、amd64 DMG 与通用 App；`git diff --check` 通过。

- [x] T5：实现额度来源三模式与旧值兼容
  - 覆盖验收标准：AC3、AC6
  - 依赖：T1、T2
  - 涉及范围：设置枚举与文案、Store 策略同步、三模式自动刷新和身份切换行为。
  - 完成条件：本机优先、仅本机、仅远程均符合 Spec，旧设置值按约定映射且不新增持久化键。
  - 验证方式：策略、设置兼容、请求计数、身份切换与手动去重定向测试。
  - 验证证据：通过；三模式请求与展示测试、未绑定/监测关闭失败边界、旧 raw value 映射及手动去重均通过。

- [x] T6：传播实际渠道并统一 HUD 与账号卡片
  - 覆盖验收标准：AC4、AC7
  - 依赖：T5
  - 涉及范围：本机快照 provenance、Store 展示数据、HUD、本机与远程账号卡片及原生截图。
  - 完成条件：四种实际渠道标识准确，远程专属字段可并存，内部 provenance 不进入外部输出。
  - 验证方式：来源传播测试、展示策略测试与合成账号原生截图。
  - 验证证据：通过；app-server、失败缓存、本地记录和稳定化均保留来源，诊断 JSON 不包含内部字段；合成截图验证三段设置、HUD 短标识及账号卡片完整来源与远程字段并存。

- [x] T7：重新完成三模式本地回归与构建门禁
  - 覆盖验收标准：AC5、AC6、AC7
  - 依赖：T5、T6
  - 涉及范围：当前未提交工作树；不使用真实凭据或联网账号，不安装或替换运行中的应用。
  - 完成条件：定向测试、完整测试、原生截图测试、构建与 diff 检查全部通过并更新证据。
  - 验证方式：执行仓库规定的全部入口，保留真实账户验收边界。
  - 验证证据：通过；定向测试 43 项，`scripts/test.sh` 通过 284 项 Swift 测试及独立回归，原生截图测试通过并人工检查，`scripts/build.sh` 生成 arm64、amd64 DMG 与通用 App，`git diff --check` 通过。

- [ ] G1：真实账户切换和长期运行验收
  - 覆盖验收标准：AC5 的实机边界
  - 依赖：T4
  - 环境前提：用户授权的真实账号、真实登录切换与足够长运行窗口。
  - 完成条件：真实环境确认身份切换、周期抑制和过期兜底没有账号串用或请求风暴。
  - 验证方式：独立实机观察；本次不执行，不以合成数据替代。

## 验收映射

| AC / 断言 | 实现责任任务 | 验证责任任务或门禁 |
| --- | --- | --- |
| AC1 / 真实采集时间传播且不外泄 | T1 | T1、T4 |
| AC2 / 本机状态与有效性 | T2、T3 | T2、T4 |
| AC3 / 自动、即时、切换、手动与去重 | T2 | T2、T4；长期行为由 G1 |
| AC4 / HUD 与卡片统一本机优先 | T3 | T3、T4 |
| AC5 / 本地门禁与实机边界 | T4、T7 | T7、G1 |
| AC6 / 三模式与旧值兼容 | T5 | T5、T7 |
| AC7 / 实际渠道传播与展示 | T6 | T6、T7 |

## 规划预检

- 结论：Ready。
- 范围：T5 至 T7 的三模式增量实现与合成验收；G1 保持独立未执行。
- 覆盖与责任：全部需求已映射到具体实现与验证任务；没有把本次实现义务转移到 G1。
- 依赖与契约：既有采集时间与状态机为基础，三模式策略先于渠道传播和统一展示；无循环依赖。
- Blocking 问题：无。

## 最终一致性 Review

- [x] Spec 的全部验收断言均有明确实现与适当验证
- [x] 所有已勾选任务满足自身完成条件且证据仍有效
- [ ] 必要集成、真实运行态与条件性门禁均已通过或有合法不适用依据
- [x] 启动依赖、验收依赖与契约就绪证据正确且无循环
- [x] 跨任务接口、类型、状态、时间、错误与副作用语义一致
- [x] 不存在未解决的 Blocking 问题、占位要求或未定义契约
- [x] 实现未超出 Spec 范围，未把可选设施变成强制要求
- [x] 证据类型、场景覆盖、代码版本与目标环境支持所声明的验收结果
- [x] 测试、配置、文档、Spec/Task 状态与实际实现一致
- [x] 必要实施 Step 已验证；提交处理符合授权且保留既有用户修改
- [x] 未发现未处理的实现、Spec、任务或验收证据不一致

### Review 结论

- 结论：通过（三模式本地实施与合成验收范围）；整体实机验收仍待 G1。
- 已确认的问题及责任任务：无。
- 尚未通过的必要门禁与阻塞原因：G1 未执行，需要用户授权的真实账号切换与长期运行窗口。
- 遗留风险或已确认的后续范围：真实账户切换、65 秒稳定期后的远程恢复和长期请求频率。
- 验证命令/过程、结果与证据引用：定向 Swift 测试、带 `CODEX_MONITOR_SNAPSHOT_DIR` 的原生截图测试、`scripts/test.sh`、`scripts/build.sh`、`git diff --check` 均通过；截图位于 `/tmp/codex-monitor-native-snapshots-quota-source-20260918`，仅为合成数据证据。
